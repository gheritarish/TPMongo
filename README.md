# TP MongoBD

## Install mongo db

### Linux

`sudo apt install mongodb python3-motor`

## Create db

In a shell use command :

`./create_db.sh`

## Insert data into db

In a shell use command:
`./insert_db.sh`
